// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("mydebug", "for mydebug support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("mydebug", "enable mydebug support", "no");

if (PHP_MYDEBUG != "no") {
	EXTENSION("mydebug", "mydebug.c");
}

